= render 'shared/headerNewUser'
%main.single-main
  %section.l--single
    %h2.l--single--head 会員情報入力
    = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = devise_error_messages!
      .l--single--inner
        .form-group
          = f.label "ニックネーム"
          %span.form-require 必須
          = f.text_field :nickname, placeholder: "例）メルカリ太郎", class: "input-default"
        .form-group
          = f.label "メールアドレス"
          %span.form-require 必須
          = f.email_field :email, autocomplete: "off" ,placeholder: "PC・携帯どちらでも可", class: "input-default"
        - if @user.email == ""
          .form-group
            = f.label "パスワード"
            %span.form-require 必須
            = f.password_field :password, autocomplete: "off", placeholder: "６文字以上", class: "input-default"
          .form-group
            = f.label "パスワード(確認)"
            %span.form-require 必須
            = f.password_field :password_confirmation, autocomplete: "off", placeholder: "６文字以上", class: "input-default"
        .form-group
          %h3.text-left.l-chapter-sub-head 本人確認
          %p.l--single__text
          安心・安全にご利用いただくために、お客さまの本人情報の登録にご協力ください。他のお客さまに公開されることはありません。
        .form-group
          = f.label "姓(全角)"
          %span.form-require 必須
          = f.text_field :family_name, placeholder: "例）山田", class: "input-default"
        .form-group
          = f.label "名(全角)"
          %span.form-require 必須
          = f.text_field :first_name, placeholder: "例) 彩", class: "input-default"
        .form-group
          = f.label "姓カナ(全角)"
          %span.form-require 必須
          = f.text_field :family_kana, placeholder: "例）ヤマダ", class: "input-default"
        .form-group
          = f.label "名カナ(全角)"
          %span.form-require 必須
          = f.text_field :first_kana, placeholder: "例）アヤ", class: "input-default"
        .form-group
          %label{for: "birthday"} 生年月日
          %span.form-require 必須
          %br/
          .birthday-select-wrap
            .select-wrap
              = select_tag :bitrh_year, options_for_select(1900..2019), { class: "select-default" }
              = fa_icon 'chevron-down', class: "icon-arrow-bottom"
            %span 年
          .birthday-select-wrap
            .select-wrap.select-wrap__month
              = select_tag :bitrh_month, options_for_select(1..12), { class: "select-default" }
              = fa_icon 'chevron-down', class: "icon-arrow-bottom"

            %span 月
          .birthday-select-wrap
            .select-wrap.select-wrap__day
              = select_tag :bitrh_day, options_for_select(1..31), { class: "select-default" }
              = fa_icon 'chevron-down', class: "icon-arrow-bottom"
            %span 月
          .clearfix
          %p.form-info-text
            ※ 本人情報は正しく入力してください。会員登録後、修正するにはお時間を頂く場合があります。
      .l--single--inner
        .form-group
          %p.l--single--text.text--center
            「次へ進む」のボタンを押すことにより、利用規約に同意したものとみなします。

          = f.submit "次へ進む", class: "btn-default btn-red"
        .form-group.text--right
          %p
            = link_to '本人情報の登録について', '#'


